# Fingerprint Pro public API key for both remotes.
# Copy this file to `.env` in `app-one/` and `app-two/`, or export the variable in your shell.
FPJS_PUBLIC_API_KEY=fpjs_public_api_key_goes_here
# Select the Fingerprint Pro region the SDK should call (e.g., us, eu, ap).
FPJS_REGION=us
